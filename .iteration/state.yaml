# 当前迭代状态（v4.3 持续周期）
cycle: 2026-08-01-v4.3-final-sprint
cycle_status: ACTIVE（R045 FREEZE 后用户 RE-FRAME "决赛评委视角优化到极限"，R046-R049 已完成）
round_next: R052

goal: >
  决赛评委 10 分钟体验旅程优化到极限：showcase 首屏、复赛帖可信度、
  演示视频一致性、移动端实机、结局页体验、PWA 离线、提交链路清洁。

baseline:
  quality: Q002（scorecard.md，R045 收敛快照）
  ui: UI-Q002（ui/baseline.md，序章场景，FREEZE）
  capability: growth.md 首期账本
  best_known_version: R048 已验证 gh-pages 4a77cf2（showcase 102.8KB/1022ms，stat-strip 8项，viewport-fit=cover）
  pre_freeze: R043 已验证 3f71178（gh-pages 2a3b0f0，线上 index-CWJb_9fJ + SW v12-prod，五页 verify 通过）

target: >
  每轮产生可证据化的 Outcome Delta（IMPROVED 且护栏稳定）；
  连续 3 个有效轮次无 Outcome Delta 触发 GATE。

guardrails:
  - 手机端 360×800 / 375×812 / 390×844 核心旅程与触摸可用性不退化
  - 桌面端 1366×768 / 1440×900 / 1920×1080 主操作与布局不退化
  - vitest 425/425、Playwright 58/58、lint 0 错、validate-story 214 节点 0 错、simulate-paths 13 策略 7 结局
  - PWA 离线链路可玩（R94 全链路基线 + R021 SW v12 递归预缓存与离线标题→序章）
  - 首访网络竞争不回退（R021 弱网：525ms 可交互、123615B、先于 Phaser）
  - 游戏加载状态不回退（R022 六视口标题/加载/完成三态隔离 + 真实 progressbar）
  - 资源失败恢复不回退（R023 六视口代码 503 双动作 + 桌面/手机图片降级说明）
  - 声音/朗读状态不回退（R024 六视口标题→游戏→暂停→标题一致性 + 无系统朗读降级）
  - 通知生命周期不回退（R025 六视口错误抢占/单活动队列 + 暂停/关闭/避让语义）
  - 随机事件与检定决策链不回退（R026 六视口影响预览 + 结果确认 + 确定性公式/后果/焦点）
  - 序章时间线与跳过控制不回退（R001/R002 基线，场景 FREEZE）

scope:
  in: 序章之后的评委旅程场景（天赋选择/剧情对话/选项面板/结局页）、首屏性能、无障碍保持、桌面全程覆盖
  out: 序章选择路口场景（FREEZE：下一触发为用户主观反馈、真实用户数据或跨系统字体差异暴露）

budget:
  wip: 3
  change: 单轮 1 主任务 + ≤2 支撑任务；同一问题连续修补 ≤2 次

environment:
  source: Windows / Vite 5 / Phaser 3 / 分支 dev-20260722
  target: GitHub Pages（niuhai.github.io/luohammer-pixel-game/）+ SW v12-prod（R039 已部署并 verify 通过）

frontend_loop:
  status: active
  protocol: .iteration/ui/continuous-loop.md
  backlog: .iteration/ui/backlog.json
  current: .iteration/ui/current.json
  command: npm run iterate:ui
  primary_viewport: 1440x900
  guardrail_viewports: 1366x768, 1920x1080, 390x844, 375x812, 360x800
  settlement: v4.3 机器字段 + 视觉前后证据 + 自动化护栏 + Outcome Contract
  next_trigger: 每次目标续跑先读取队列并推进最高价值 eligible 任务

pending_from_v3:
  - 复赛帖数字对齐已在 910519c 完成（95 轮口径）；后续轮次变更时同步刷新
  - Phaser 原始包仍约 1.48MB，但 R021 已移出首屏可交互关键路径；只有 RUM、Safari 或低端实体机证据显示空闲加载仍有问题时重开
