# R046 · showcase 图片 404 修复 + OG 社交分享标签（FREEZE 重开）

> 日期：2026-08-01 · 类型：RE-FRAME 触发式 · 触发依据：用户"优化到极限"+决赛评委视角 = RE-FRAME 信号（R045 重开条件④）

## 背景

FREEZE 后用户提出"决赛评委视角优化到极限"目标，触发 RE-FRAME。PREFLIGHT 发现 showcase 页面 5 张关键图片 404 + 无社交分享 meta 标签，影响评委首印象。

## 改动

1. **5 张图片 404 修复**：
   - 立绘路径 `../public/assets/` → `../assets/`（Vite 构建不保留 public/ 前缀）
   - 4 张截图路径 `../screenshots/` → `./screenshots/`（showcase 自包含）
   - .gitignore 加 `!showcase/screenshots/` 例外规则
2. **OG/Twitter Card meta 标签**：
   - og:title / og:description / og:image / og:url / og:site_name / og:locale
   - twitter:card / twitter:title / twitter:description / twitter:image
3. **部署脚本修复**：deploy-ghpages.cjs 显式拷贝源 showcase/ 到中转目录（R040 教训：dist/showcase 空目录会让 /MIR 删成 404）

## 验证

- 线上 showcase 5 张图片全 200（Invoke-WebRequest HEAD 验证）
- OG 标签线上可访问（`$r.Content -match 'og:image'` = True）
- dev 555be65 + b808c60 已 push；gh-pages 119c9c7 已 push

## 结果增量：IMPROVED

- 价值结算：V2（评委分享 showcase 链接时显示预览图+标题+描述，首印象提升）
- 证据等级：E2（线上实机验证）
- 护栏：PASS（游戏主体无改动，仅 showcase 独立目录）

## 残余风险

- showcase 页面体积仍 1337KB（base64 背景图），加载 9s → R047 处理
