# R043 · 全自动真实通关（无注入，序章→六阶段→结局）

> 日期：2026-07-31 · 类型：SCAN 验证轮（产品完成度终极实证）· 视角轮换依据：此前所有专项均为注入式/分段式，"从标题到结局真实走完全程"从未覆盖；评委第一印象就是完整玩一局

## SCAN

探针 `scripts/_probe_r043_fullrun.cjs`（390×844 竖屏，localStorage 清空起步，自带 4189 preview 跑生产构建）：
标题→开始游戏→天赋翻牌 5 选 2→主循环（消解中断层/点选项/空格推进）→结局 overlay。

### 排障记录（两个探针侧根因，游戏代码零改动）

1. **"压力崩溃死循环"假象**：首轮跑到 act1_preputation 时探针反复报告"压力到达极限"层不消失，疑似游戏死循环。诊断脚本 `_diag_r043_consequence_loop.cjs` dump 现场后定位：探针 `click(cons.querySelector('.ui-consequence-continue')) || click(choice)`——**合成 `dispatchEvent` 不受 `hidden` 属性约束**，decision 阶段隐藏的 continue 按钮被命中，`_runAdvance()` 因 `_advance=null` 空转返回 true，真正的崩溃恢复选项永远点不到。游戏崩溃恢复链（showDecision→choice→showResult→continue→`_proceedAfterChoice`）本身完整无 bug。修复：continue 加可见性门（`!hidden && offsetHeight>0`）。
2. **贪首选项提前收官**：修复后首轮通关但 39 选即达"工匠传奇"（act4 中场结局），settlements=2 < F4 门槛 3，六阶段覆盖不全。升级选项策略：按钮序与 `ChoiceSystem.choices` 一一对应，优先点 `next` 不直达 `ending*` 的可用选项，全部直达结局时才进结局。

## 结果：6/6 PASS

| 检查 | 结果 |
|---|---|
| F1 天赋 overlay 可达 | PASS（翻牌揭晓→5 选 2→确认） |
| F2 真实通关到达结局 | PASS（choices=74 settlements=5 randomEvents=9 consequences=30 checkAnims=2，耗时 516s） |
| F3 决策密度 ≥12 | PASS（74 次真实选择） |
| F4 阶段结算 ≥3 | PASS（5 次，六阶段全走到） |
| F5 结局内容非空 | PASS（"▲ 浴火重生·真还传本传"——"我不赖账，我不破产，我一分不少地还。"） |
| F6 全程零 pageerror/console.error | PASS |

途中实经：压力崩溃恢复（consequences=30，含修复前的假循环点位真实通过）、阈值事件、属性检定（checkAnims=2）、随机事件（9 次）、阶段结算（5 次，截图 r043-settlement-1~5.png）+ 结局截图 r043-ending.png。

观察（非缺陷）：historyNotes=0——历史真相浮层不在自动推进路径上（需玩家主动点"历史真相"入口），与 R042 更多菜单验证互补，不另立轮次。

## SETTLE

- Outcome：评委最核心动作"完整玩一局"在生产构建上端到端实证——序章→天赋→六阶段→真还传结局全通，74 次真实选择零报错；两个探针假象（崩溃死循环/提前收官）消解，游戏逻辑证明健壮
- Value：C（验证轮，游戏代码零改动；探针健壮性+覆盖策略升级沉淀为可复跑资产）
- Evidence：E2 实机探针 exit 0 + 截图 6 张（test-screenshots/r043-*.png）
- 收敛计数：无 A/B 轮次 = 4（R041+R042+R044+R043）——按机制进入收敛复核
- Decision：GATE——覆盖矩阵全行"新鲜"，连续 4 轮无 A/B 改进项，执行收敛复核；复核通过后转触发式轮次（仅覆盖矩阵"下次触发"条件命中时开轮）
