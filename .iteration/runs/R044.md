# R044 · PWA 离线全链路实证（部署版穷举审计）

> 日期：2026-07-31 · 类型：SCAN 验证轮（平行会话 R043 全通关在跑，本轮走正交视角）· 视角轮换依据：README/复赛帖对外声称"离线全链路可玩"，评委可直接断网测试；R94/R021 后历经 R005~R042 改动，部署版 v12 SW 缺一次穷举级复核

## SCAN

探针 `scripts/_probe_r044_offline.cjs`（390×844，自带 preview，当前部署版 dist=index-CWJb_9fJ）：

两层证据设计：
1. **穷举审计**：SW install 后直读 cache keys，与 dist/assets 全量 11 项 JS/CSS 比对 + 读 `__precache_failures__` 调试键
2. **实机断网**：`setOffline(true)` 后 reload → boot → 天赋（IntroScene chunk）→ GameScene 首对话框（主 chunk）

排障记录：静态分析曾误报"8/10 chunk 未在入口引用→预缓存不全"，精读 [sw.js](file:///e:/ownWorkPlace/罗的十字路口/luohammer-pixel-game/public/sw.js#L90-L116) 确认 R021 已做 BFS 递归发现（入口→loader→懒加载 chunk 全图），误报消解——本轮以运行时证据复核该设计。

## 结果：7/7 PASS

| 检查 | 结果 |
|---|---|
| O1 SW 激活且版本正确 | PASS（luohammer-v12-prod） |
| O2 precache 覆盖 dist/assets 全部 JS/CSS | PASS（11/11 全在缓存，含入口不直接引用的 8 个懒加载 chunk） |
| O3 `__precache_failures__` 为空 | PASS |
| O4 离线重载 boot overlay 可见 | PASS |
| O5 离线推进到天赋 overlay | PASS（IntroScene chunk 离线可用） |
| O6 离线进入 GameScene 首对话框 | PASS（主 chunk 离线可用） |
| O7 全程零 pageerror | PASS |

## SETTLE

- Outcome：对外声称"离线全链路可玩"在部署版上从设计声明升级为穷举级实证；递归预缓存设计（R021）经运行时验证有效
- Value：C（验证轮，无代码改动；守住一条评委可直接测试的对外声称）
- Evidence：E2 实机断网测试 + 缓存键穷举比对（探针 exit 0）
- 收敛计数：无 A/B 轮次 = 3（R041+R042+R044，R043 在跑不计）——若 R043 亦无 A/B，按机制进入收敛复核
- Decision：NEXT——等 R043 全通关结论后复核收敛判定；离线链路下次触发条件：SW/入口图/manifest 变化
