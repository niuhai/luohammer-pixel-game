# R004 · 天赋系统测试卫生与跨周目生命周期加固

- 日期：2026-07-28
- 决策：CONTINUE
- 结果增量：IMPROVED（测试基线修复，护栏恢复全绿）
- 价值结算：V1（质量护栏修复，无用户可见功能变更）
- 证据：E1（自动化测试全绿）
- 严重度：S1
- 变更风险：R0（仅修改测试文件 + 补全 destroy 调用）

## Outcome Contract

- 触发：PREFLIGHT 阶段 `npx vitest run` 发现 1 项失败（346/347 通过）。
- 目标：恢复 vitest 全绿护栏，确保天赋系统翻牌动画与跨周目销毁逻辑被正确覆盖。
- 基线：talent-system.test.js 第二个用例 combo 文本断言失败，收到空字符串。
- 目标状态：18 个测试文件、348 项全部通过；lint 0 错；build 通过；validate-story 通过。
- 回退：仅 1 行 destroy() 补全，可独立回退。

## 诊断与选择

1. 失败测试在单独运行时通过，与第一个测试（跨周目刷新按钮）连跑时失败→测试间污染。
2. 根因：第一个测试创建 secondSystem 后未调用 destroy()，未清理的定时器回调在第二个测试的 fake clock 环境中造成 comboEl 更新被跳过或覆盖。
3. 选择最小修复：在第一个测试末尾补全 `secondSystem.destroy()`，确保跨周目实例生命周期完整闭环。
4. 不修改业务代码：TalentSystem 翻牌动画（is-dealing → is-revealed 状态机）与 combo 显示逻辑在独立测试中验证正确。

## 改动

- `test/unit/talent-system.test.js`：在"销毁旧实例后"测试用例末尾添加 `secondSystem.destroy()`，确保第二个 TalentSystem 实例被正确销毁；
- 该修复同时验证了翻牌动画的禁用→启用状态机（第三个测试用例，已在工作区中）在完整生命周期下正确工作。

## 证据

- vitest：18 files, 348/348 passed（之前 346/347）
- ESLint：0 errors, 0 warnings
- Vite production build：通过
- validate-story：0 errors, 0 warnings, 214 节点通过
- simulate-paths：13 策略模拟完成，无异常

## 验证

- PREFLIGHT：git status 确认工作区含未提交改动（TalentSystem 翻牌动画重构、effects.js、talents.js 等），属上一轮迭代遗留，本轮未触碰业务代码；
- vitest 全量：348/348 ✅；
- build：dist 产物 73 modules，无错误 ✅；
- lint：--max-warnings 0 通过 ✅；
- 故事数据完整性：0 错 0 警告 ✅。

## 品质与成长结算

- 质量 Delta：测试护栏从 1 项失败恢复至全绿；新增翻牌动画锁定测试（is-dealing → is-revealed）覆盖了 R28 以来的抽卡仪式感动画；
- 性能护栏：稳定，无代码变更影响产物体积；
- 成长种子：
  - `SEED`：跨周目系统实例必须在测试中显式 destroy()，否则 fake timers 环境下定时器回调会污染后续用例；
  - `SEED`：TalentSystem 翻牌动画（disabled → enabled, is-dealing → is-revealed）有独立测试锁定（"翻牌完成前不可误选"用例）；
- 护栏基线更新：vitest 通过数 347 → 348（新增翻牌锁定测试）。

## 下一触发

- 天赋选择界面在真实浏览器中出现 combo 显示异常；
- 跨周目（reroll/新局）场景出现 DOM 引用泄漏或事件监听器重复绑定；
- 后续 UI 迭代引入新的测试污染模式。
